# BACKEND ENVIRONMENT VARIABLES

# DATABASE_URL=
DATABASE_URL=
# DATABASE_URL=
PORT=4000
FRONTEND_URL=http://localhost:3000
SUPER_NAME=""
SUPER_PASSWORD=""
JWT_SECRET=
JWT_EXPIRY=60m
SMTP_EMAIL=grrocket117@gmail.com
SMTP_PASSWORD=
EMAIL_INTERNAL_KEY=
DOC_ENABLE=true

# --- Face recognition ---

### OpenFace CLI (single-container backend)
FACE_ENGINE=openface-cli
# OPENFACE_CLI_PATH=/opt/OpenFace/build/bin/FeatureExtraction
# OPENFACE_TIMEOUT_MS=20000
# OPENFACE_MIN_CONFIDENCE=0.90
# FACE_MATCH_THRESHOLD=0.85                        # Optional cosine threshold (higher is stricter)

